const heroProps = {
    title: 'Education',
}

const degreeProps = {
    title: "Degrees Received"
}

const certificationProps = {
    title: "Certifications"
}

export const PROPS = {
    HERO: {
        ...heroProps
    },
    DEGRESS: {
        ...degreeProps
    },
    CERTIFICATION: {
        ...certificationProps
    }
}
